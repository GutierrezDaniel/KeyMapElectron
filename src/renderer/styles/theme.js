import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#3d0e3a',
    },
    secondary: {
      main: '#f7cea5',
    },
    background: {
      default: '#4c5b5c',
    },
  },
});
